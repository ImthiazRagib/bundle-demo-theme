{% layout 'theme' %}

<section class="page-content" style="text-align:center; padding:100px 24px;">
  <div class="page-content__container" style="max-width:480px;">
    <p style="font-size:80px; font-weight:800; color:var(--color-border); line-height:1; margin-bottom:16px;">404</p>
    <h1 style="font-size:28px; font-weight:800; margin-bottom:12px;">Pagina non trovata</h1>
    <p style="color:var(--color-text-muted); margin-bottom:32px;">La pagina che stai cercando non esiste o è stata spostata.</p>
    <a href="{{ routes.root_url }}" class="bundle-intro__cta">← Torna alla Home</a>
  </div>
</section>
